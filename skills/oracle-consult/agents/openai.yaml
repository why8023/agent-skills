interface:
  display_name: "Oracle Consult"
  short_description: "Controlled Oracle consulting workflow"
  default_prompt: "Use $oracle-consult to decide whether Oracle is justified, run dry-run first, keep Oracle browser runs on --browser-model-strategy current, use both --timeout 90m and --browser-timeout 90m for Deep Research, and summarize verified findings."
policy:
  allow_implicit_invocation: true
