interface:
  display_name: "Skill Sync"
  short_description: "Sync repo skills through git and npx skills"
  default_prompt: "Use $skill-sync to create, update, or rename a skill in this repository, push it to the remote repo, and then refresh the matching local agent installs via npx skills."
