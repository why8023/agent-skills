interface:
  display_name: "Skill Authoring Sync"
  short_description: "Update repo skills, then push and sync to multiple agents"
  default_prompt: "Use $skill-authoring-sync to create or update a skill in this repository, commit and push it, and then refresh the default multi-agent installs from that remote via npx skills."
